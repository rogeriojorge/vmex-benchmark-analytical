# Local-agent continuation prompt

Continue the existing `rogeriojorge/vmex-benchmark-analytical` repository. Read `plan_next.md`, `REVIEW_FINDINGS.md`, `REFERENCES.md`, `review_snapshot.json`, the actual current plan and the newest logbook before editing. The reviewed checkpoint is `05e947370b9d0cebc0e508361e447b03523a331d`. Preserve later work, all raw results, amendments, archived plans and scientific attribution. Archive the current plan byte-for-byte before adopting the new contract; keep one active plan and append a continuing logbook.

Do not repeat tasks already completed: reference CI is green, the import/schema repair is done for the recorded cases, the historical NS129 zero-TCON run exists, and the preconditioned single-grid history test passed. Full physical scoring and response certification remain open.

Begin with C0. Repair `_root_and_anchor` so it observes a raw host state and one actual refinement, rather than measuring after `solve_implicit_with_aux` has already attempted refinement and calling its memo again. Record real corrections and residuals. Correct dimensionless-parameter field scales and sample-count-dependent response norms. Keep historical observations unchanged and give amended interpretations separate records. Capture runtime source bytes or a complete committed-tree/patch identity before expensive execution.

Then perform two bounded C1 diagnostics using saved states: the default NS65 branch tangent's defect in one frozen residual, and one genuinely observed refinement of the zero-TCON base. Do not regenerate plus/minus branches before the base root and matched operator are certified. Do not call the radial m=1 alignment a gauge: its field-invariance test failed. A returned state and a converged linear solve are not automatically a differentiable equilibrium.

In parallel, rescore the saved promising NS129 candidates with correct domain/native-knot semantics. Run the full-flux lambda prototype against current reference files and compare it with the existing sheared-A transport solution. Keep projection, root and full physical-field verification separate. The delivered prototype results used the initially supplied analytical reference and are not current VMEX measurements.

Continue through the plan's nonzero/null responses, sheared-A and meaningful LASYM recovery, both current/iota closures, exact-flow and diagnostic interfaces, exterior operators, strict coupled free-boundary tests, bounded polishing and direct-family optimization. Do not let one difficult case block independent reference/interface or direct-family work. Do not merge PR448, change the global TCON default or rewrite the solver before the discriminating tests justify a specific action.

Keep code small and deliberate. Reuse existing reference, measurement, evidence and adapter helpers. Add tests for actual physical and contract failures, not just mocks or symbol existence. Use finite process budgets, checkpoint every completed expensive child result, and preserve failures. Never manufacture a passing status by clipping bad samples, hiding root failures or relabelling a different residual as the same equation.

Verify GitHub authentication is exactly `rogeriojorge`. Configure both author and committer locally with the verified owner identity; do not change global Git configuration. All new commits, pushes, issues and PR comments must use that account, with no assistant authors or co-author trailers. Keep third-party licenses and references. Inspect publication helpers before use; a helper pushing main must not accidentally publish a topic branch to the wrong destination. Upstream fixes belong in separate narrow PRs; do not merge them or push directly to upstream default branches.

After each meaningful block, update the C0-C8 table and append the question, source/operator hashes, effective command, root/physical/derivative status, measurements, failed attempts, artifacts, branch/PR state and exact next action. Leave a replacement agent able to resume without this conversation.
