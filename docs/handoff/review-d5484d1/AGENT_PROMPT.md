# Local-agent prompt

Continue the existing public repository `rogeriojorge/vmex-benchmark-analytical` using `plan_next.md`, `REVIEW_FINDINGS.md` and `REFERENCES.md` from this handoff. The reviewed main is `d5484d1e7a15c9cf10599c2b0f05bdf69e22e861`; compare your actual checkout and remote with it. Preserve all later work, the active plan's entire logbook, prior histories and raw numerical artifacts. Do not recreate the repository or reset a dirty tree. Adopt this plan only after reconciling newer instructions, and leave a link to the archived preceding plan.

The latest work repaired much of the old evidence handling and added useful independent measurements and input-map finite differences. Retain those repairs. The immediate problem is not simply 'use more quadrature points': the current composite rule partitions reference flux instead of actual native interpolation knots, has NS33-specific node counts, permanently requires inaccurate coarse rules to pass, and loses completed grid results on KeyboardInterrupt. The current reference CI fails at pytest; pure tests import a VMEX-dependent driver although the declared reference environment does not install VMEX. The new runner and scorer also disagree on report schema.

Begin with T0 and the cheap parts of T1:

1. Repair the solver-independent import boundary and run reference tests in a clean environment with only declared reference dependencies. Inspect the current CI traceback. Do not hide pure tests behind import skips or add the whole optional solver stack to the reference environment.
2. Implement a small tested schema-1/schema-2 reader and keep raw records unchanged. Test the actual new runner-to-scorer contract, source identity, effective controls and capped states.
3. Test a displaced reference/native chart, derive node counts from actual breakpoints and order, and implement checkpointed streaming reductions. Add a deliberate interruption test before running a large grid. Do not repeat the interrupted 27-minute composite command unchanged.
4. Evaluate native-cell norms with an explicit domain, a sparse direct-Cartesian audit and an independent covariant-curl check. For a common reference-domain norm, find the true native-knot crossings or independently control the integration error. Preserve reference and native domains as distinct until their mismatch is within budget.
5. Use a bounded diagnostic estimate for the already-failed NS33 state. Keep stricter acceptance tolerances for recovery, and do not block all work on estimating an order-one failure to 1e-4 absolute precision.

Then complete the missing historical NS129 projected TCON0=0 experiment once in a unique run, test reusable-residual history independence, and compare raw physical/constraint force vectors and physical error terms. Preserve historical VMEX `b5f5267...` separately from current `926892a...`; do not silently mix them in a convergence plot. Do not change a global constraint default or rewrite the solver before those discriminating tests.

In parallel, save signed, dimensionless block-scaled tangents from the complete axisymmetric input map and carry them into a certified VMEX residual tangent/adjoint calculation. A verified finite-difference input tangent is an acceptable first step; label it correctly. Check both a nonzero physical response and the integer family's zero Eulerian B response to delta. Begin sheared-A preparation/recovery rather than allowing the integer 3-D case to block every phase. Current-prescribed transform must be predicted, not imposed, in its lambda-null test.

Run the plan's exact-flow, field-derivative, meaningful LASYM, diagnostic and scoped adjacent-code tests as their assumptions permit. Direct exact-reference tests may proceed before nonlinear recovery. Preserve all evidence distinctions. An exact interior does not supply a vacuum exterior; a cubic geometry does not guarantee arbitrary global field derivatives; a reverse-only wrapper does not prove forward-mode support.

VMEX PR448 and VMEC++ PR849 are optional pinned comparisons, not dependencies to merge. PR448's provisional magnetic-normalized force number is not an accepted result and is not the analytical field error E_B. Respect its axisymmetric fixed-profile scope and memory limitations.

Keep changes small, deliberate and readable. Reuse existing pure/evidence/measurement modules, VMEX operators and SOLVAX solvers. Avoid one script per grid, copied physics, a workflow framework, or an unrestricted multi-code campaign. Record original-operator residuals, coordinate/representation gates, uncertainty, failed attempts, actual time/RSS and unknown device memory. Generate figures from immutable data; plotting must never repair primary evidence. Store large future arrays as checksummed release assets when appropriate, preserving existing Git history.

Verify `gh api user --jq .login` is `rogeriojorge`. Configure author and committer only locally as `rogeriojorge`, using the verified owner email or account-derived noreply address. All new commits, pushes, comments and PRs must use that account, with no automated-assistant authors or co-author trailers. Keep inherited authorship and third-party scientific attribution intact. Inspect `tools/publish.sh` before using it: do not let its historical broad allowlist or direct-main behavior override this branch's review. Make narrow upstream fixes in separate worktrees/PRs; do not merge them or push to upstream main.

After each meaningful block update the current task table and append a logbook entry with source/input/state/grid hashes, exact commands and effective controls, measurements, failures, artifacts, branch/PR state and the exact next action. Preserve completed work on interruption. A replacement agent must be able to resume from the repository alone. Do not mark an unrun, unresolved or provisional experiment as passed.
