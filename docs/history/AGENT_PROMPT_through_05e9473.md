# Local implementation prompt

Work in this extracted repository. Read `README.md`, `plan.md`, `docs/SOURCE_REVIEW.md`, `sources.json`, and the current logbook before changing anything. The canonical task and acceptance criteria are in `plan.md`.

Create and maintain the public repository `rogeriojorge/vmex-benchmark-analytical` using the owner's existing GitHub authentication. Verify that the authenticated account is `rogeriojorge`. All new commits, pushes, comments and PRs must use that account; configure both Git author and committer locally as `rogeriojorge`, with a verified owner email or the account's correctly derived noreply address. No automated-assistant authors or co-author trailers. Preserve scientific references and third-party licenses. Do not change global Git identity or rewrite upstream history. Inspect `tools/publish.sh` and its staged diff before invoking it with `PUBLISH=1`.

Implement the phases in the plan, starting with the source/API audit, reference tests, sign/profile conversion and native-state field sampling. This bundle has measured analytical reference checks and candidate input files, not completed VMEX benchmarks. Do not report the existing reference figures as solver results. The B/C input fits are unresolved and must be refined before using them for recovery. The current VMEX code has free-boundary root anchoring despite an older contradictory validation paragraph; test its actual paths and certificates.

Use separate pinned worktrees for baseline VMEX and proposed changes. Review the full relevant source and tests locally, completing the per-file review ledger rather than calling a README or AST inventory a code audit. Resolve all used dependency pins. Reuse existing VMEX, SOLVAX, Boozer, virtual-casing and field interfaces. Add small shared adapters and experiment scripts, not a new framework or copied solver. Any necessary upstream fix belongs in its own narrow PR; do not merge it or push directly to an upstream main branch.

Cover exact axisymmetric and 3-D fixed-boundary equilibria; LASYM=F/T controls; genuinely asymmetric Solov'ev physics; exact hidden-symmetry coordinate tests; both current and transform closures; field derivatives; complete-family implicit sensitivities; force/current and diagnostic convergence; anchored free-boundary numerical references; vacuum/exterior operator checks; bounded polishing; optimization and tangent/transverse continuation. Use independent oracles and label numerical, asymptotic, integration and unavailable cases honestly. An exact interior equilibrium is not automatically an exact free-boundary solution. Do not assume forward-mode or Hessian support through a reverse-only API.

Run the supplied tests and reference scripts first. Establish each phase's physical and derivative gates before launching large runs or optimization. Use actual nonlinear, anchor and adjoint residuals; compare reconverged finite differences over multiple steps and spatial resolutions. Record unsuccessful attempts, skipped dependencies, source changes, runtime and memory. Never turn an invalid field, failed solve or uncertified derivative into a passing number.

Keep source code, docstrings and comments clear, short and deliberate. The README should show measured results, their limitations and exact run commands. Generate figures from saved arrays with provenance, inspect the images, and keep numerical summaries small. Large states belong in checksummed release assets when needed, not bloated Git history.

Update the phase table and append a logbook entry after every meaningful work block. Leave exact commands, run IDs, artifacts, branch/PR state, failures and the next action so another agent can resume. Continue through the phases while their prerequisites are satisfied. When a real blocker remains, diagnose and record it, complete independent work that is still possible, and hand over the smallest concrete next experiment rather than inventing completion.
